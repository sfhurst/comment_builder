create an array or object that can hold another one with these things: 
number press or equivalent number based on quantity and severity fields, quantity, severity, defect, location, details, template, reported statement

clicking forward arrow makes a new array unless not on the last array.
clicking backward navigates back one array until it is at the last array and then it does nothing and is grayed out.

clicking clear deletes the current array

clicking close and copy, copies the final text to the clipboard and closes the browser window
maybe output comments to a text file as a log