make another set of templates for a period before the details. capitalize the details sentence.
change the color of the template buttons when the setIndex changes.
save the setIndex to the array objects so that these templates are correct.
match the template preview text to the setIndex for that page. maybe. Maybe just do the buttons.

clicking close and copy, maybe output comments to a text file as a log

clicking the template buttons slightly tweak the layout.
this will allow you to build the templates around punctuation.
maybe they all link so clicking any makes them all fit if they have similar things in common like leading with the.
adjust the details separately so it can be placed in different ways. maybe.